<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android7learn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_mosii_human"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_mosii_human_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RecyclerViewSample" />
        <activity android:name=".ViewsSample" />
        <activity android:name=".ConstraintLayoutSampleActivity" />
        <activity android:name=".FrameLayoutSampleActivity" />
        <activity android:name=".RelativeLayoutSampleActivity" />
        <activity android:name=".LinearLayoutSampleActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>